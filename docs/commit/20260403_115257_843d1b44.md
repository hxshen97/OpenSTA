# 波形传播端到端回归测试

- **Time**: 2026-04-03 11:52:57
- **Commit**: 843d1b44
- **Branch**: master

## Features

### 波形传播回归测试
新增 `waveform_propagate` 回归测试，验证波形传播引擎在真实设计上的端到端正确性。
使用 `reg1_asap7` 设计（DFF + BUF + AND2 组合路径），VCD 包含 DFF 输出内部信号（r1/Q、r2/Q），
验证 BUF 和 AND2 门能正确传播翻转事件。

### 内部信号 VCD 测试数据
新增 `reg1_asap7.vcd` 测试波形文件，包含顶层端口和 DFF 输出内部信号的翻转数据，
为组合逻辑传播提供种子事件。
